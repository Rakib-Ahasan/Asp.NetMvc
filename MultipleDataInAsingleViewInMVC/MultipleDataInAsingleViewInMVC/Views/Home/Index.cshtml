@using System.Web.UI.WebControls
@using MultipleDataInAsingleViewInMVC.Models
@model MultiModelData
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@{
    Html.RenderAction("Students");

}

@{
    Html.RenderAction("Teachers");
}

@*<h1>STUDENT DATA</h1>
    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Gender</th>
                <th>Class</th>
                <th>Fees</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in (List<Student>)ViewData["student"])
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.name</td>
                    <td>@item.gender</td>
                    <td>@(item.@class)</td>
                    <td>@item.fees</td>
                </tr>
            }
        </tbody>
    </table>
    <h1>TEACHERS DATA</h1>
    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Gender</th>
                <th>Designation</th>
                <th>Salary</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in (List<Teacher>)ViewData["teacher"])
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.name</td>
                    <td>@item.gender</td>
                    <td>@item.qualification</td>
                    <td>@item.salary</td>
                </tr>
            }
        </tbody>
    </table>*@







