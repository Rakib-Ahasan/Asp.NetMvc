@model List<StronglyTypePartialViewInAsp.NetMVC.Models.Product> 
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<h1 class="text-center">PRODUCTS</h1>

@{
    foreach (var item in Model)
    {
        Html.RenderPartial("_ProductPartialView", item);
    }
}